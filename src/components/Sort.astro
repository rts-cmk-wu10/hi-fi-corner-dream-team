<script>
  import { gridLayoutChanger } from "../scripts/utils";

  gridLayoutChanger()
</script>

<div class="sort__div">
  <div class="sort__by__con">
    <p class="p--upper">sort by:</p>
    <select name="sort__by" id="select__sort">
      <option value="offer">Offer</option>
      <option value="price-ascending">Price ascending</option>
      <option value="price-descending">Price descending</option>
      <option value="name">Name</option>
    </select>
  </div>
  <div class="view__as__con">
    <p class="p--upper">view as:</p>
    <i class="fa-solid fa-table-cells-large"></i>
    <i class="fa-solid fa-list"></i>
  </div>
  <p class="sort__by__number__items"></p>
  <div class="sort__by__numbers">
    <p class="p--upper">show:</p>
    <select name="show__numbers" id="select__show">
      <option value="0">No limit</option>
      <option value="05">5</option>
      <option value="10">10</option>
      <option value="20">20</option>
    </select>
  </div>
</div>

<style>
  .sort__div {
    display: grid;
    grid-template-columns: 2fr 1fr 2fr 1fr 1fr;
    align-content: center;
    background-color: var(--filter-bg-color);
    padding: 2em;
    gap: 1em;
  }

  .sort__by__con, .view__as__con, .sort__by__numbers {
    display: flex;
    align-items: center;
    gap: 0.5em;
  }

  .fa-list, .fa-table-cells-large {
    color: var(--text-color);
    cursor: pointer;
    transition: transform 0.2s;
  }

  .fa-list:hover, .fa-table-cells-large:hover {
    transform: scale(1.25);
    color: goldenrod;
  }

  .sort__by__numbers {
    grid-column: 5 / 6;
  }

  .sort__by__number__items {
    grid-column: 4 / 5;
    justify-self: center;
  }
</style>